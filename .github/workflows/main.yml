name: Generar Reporte LTV Senior

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Descargar repositorio
      uses: actions/checkout@v3

    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Instalar librerías
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl matplotlib seaborn fpdf

    - name: Ejecutar Pipeline de Datos y Reporte PDF
      run: python scripts/procesar_ltv.py

    - name: Cargar Reporte Final
      uses: actions/upload-artifact@v4
      with:
        name: LTV_Executive_Report
        path: reportes/LTV_Report_2023_2025.pdf